import styled from 'styled-components';

const StyledWaitingRoom = styled.div`
    p {
        text-align: center;
        font-weight: bold;
        font-size: 20px;
    }
`;

export default StyledWaitingRoom;
